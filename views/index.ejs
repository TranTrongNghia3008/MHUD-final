<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <style>
        body {
            height: 100vh;
            justify-content: space-between;
        }
        #options {
            flex-basis: 300px;
            padding: 20px;
            border-left: 1px solid #000;
        }
        #video_grid {
            flex-grow: 1;
            display: flex;
            flex-wrap: wrap;
            gap: 2%;
            padding: 2%;
            height: 100vh;
            overflow: scroll;
        }
        video {
            width: 45%;
            height: 45vh;
            object-fit: cover;
        }
    </style>

    <script>
        const roomId = "<%= roomId %>";
    </script>

    <link
        rel="stylesheet"
        href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css"
    />

    <script src="/socket.io/socket.io.js"></script>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
</head>
<body class="d-flex">
    <div id="video_grid">
      <!-- <div id="imageContainer">
        <img id="receivedImage" src="" alt="Received Image">
      </div> -->
    </div>
    <div id="options">
      <p style="font-weight: bold" class="mb-1 mt-2">Controllers</p>
      <button id="muteBtn" class="btn btn-primary">Mute yourself</button>
      <button id="cameraoff" class="btn btn-primary">Turn off camera</button>
      <p style="font-weight: bold" class="mb-1 mt-2">Change Camera</p>
      <select
          id="selectCam"
          style="width: 300px; margin: auto"
          class="form-select"
          aria-label="Default select example"
      ></select>
      <p style="font-weight: bold" class="mb-1 mt-2">Change Mic</p>
      <select
          id="selectMic"
          style="width: 300px; margin: auto"
          class="form-select"
          aria-label="Default select example"
      ></select>
      <p style="font-weight: bold" class="mb-1 mt-2">Share Your Screen</p>
      <button id="screenShare" class="btn mt-2 btn-secondary">Share your Screen</button>
      <input
          type="text"
          id="messageInput"
          placeholder="Enter message to embed"
      />
      <button id="sendMessageBtn" class="btn btn-primary">Send Message</button>
      <p style="font-weight: bold" class="mb-1 mt-2">Upload File</p>
      <form id="uploadForm" enctype="multipart/form-data">
        <input type="file" name="file" id="fileInput" /> <!-- Đặt id="fileInput" để dễ dàng chọn bằng JavaScript -->
        <button type="button" id="uploadBtn" class="btn btn-primary mt-2">Upload File</button> <!-- Đổi type="submit" thành type="button" -->
      </form>
      <div id="uploadedFiles">
        <!-- Files will be listed here -->
      </div>
    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/crypto-js/4.0.0/crypto-js.min.js"></script>
    <script type="module" src="./app.js"></script>
    <script type="module" src="./steganography.js"></script>

    <script>
        $(document).ready(function () {
            $('#uploadForm').on('submit', function (e) {
                e.preventDefault();
                var formData = new FormData(this);
                $.ajax({
                    url: '/upload',
                    type: 'POST',
                    data: formData,
                    contentType: false,
                    processData: false,
                    success: function (response) {
                        alert('File uploaded successfully!');
                        // window.location.href = "/" + roomId; // Redirect back to the original page
                    },
                    error: function (jqXHR, textStatus, errorThrown) {
                        alert('File upload failed!');
                    }
                });
            });
        });
    </script>
</body>
</html>
